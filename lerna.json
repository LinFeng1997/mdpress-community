{
  "npmClient": "yarn",
  "useWorkspaces": true,
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": ["master", "dev"],
      "conventionalCommits": true,
      "exact": true,
      "ignoreChanges": ["**/*.md", "packages/docs/*"],
      "message": "build: publish"
    }
  }
}
